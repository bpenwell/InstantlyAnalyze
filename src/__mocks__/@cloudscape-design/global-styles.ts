export enum Mode {
  Light = 'light',
  Dark = 'dark',
}

export enum Density {
  Comfortable = 'comfortable',
  Compact = 'compact',
}

export const applyMode = (mode: Mode) => {};
export const applyDensity = (density: Density) => {}; 